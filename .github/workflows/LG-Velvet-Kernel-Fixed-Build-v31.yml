name: LG Velvet 5G - Stock Kernel + KernelSU + SUSFS

on:
  workflow_dispatch:
    inputs:
      defconfig:
        description: "Kernel Defconfig"
        required: true
        default: "vendor/caymanlm_defconfig"

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-22.04
    timeout-minutes: 90

    env:
      ARCH: arm64
      LLVM: "1"
      LLVM_IAS: "1"
      CLANG_DIR: ${{ github.workspace }}/clang-prebuilt/clang-r547379
      GCC64_DIR: ${{ github.workspace }}/gcc64
      GCC32_DIR: ${{ github.workspace }}/gcc32
      DIAG_DIR: ${{ github.workspace }}/ci-logs
      KSU_VERSION: "v0.9.5"
      SUSFS_VERSION: "1.4.2-kernel-4.19"

    steps:

      - name: 01 - Checkout Stock Kernel Source
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: 02 - Initialize Diagnostics
        shell: bash
        run: |
          set +e
          mkdir -p "$DIAG_DIR"
          {
            echo "========== STOCK KERNEL BASELINE =========="
            echo "DATE=$(date -u '+%Y-%m-%d %H:%M:%S UTC')"
            echo "RUN_ID=$GITHUB_RUN_ID"
            echo "SHA=$GITHUB_SHA"
            echo "BRANCH=$GITHUB_REF_NAME"
            echo
            git rev-parse HEAD
            git status --short
            echo
            grep -E '^(VERSION|PATCHLEVEL|SUBLEVEL|EXTRAVERSION)[[:space:]]*=' Makefile
            echo
            echo "Defconfig input: ${{ github.event.inputs.defconfig }}"
            echo
            df -h
            echo
            free -h
          } > "$DIAG_DIR/02-initialize.log" 2>&1

      - name: 03 - Install Build Dependencies
        shell: bash
        run: |
          set +e
          LOG="$DIAG_DIR/03-install-dependencies.log"
          : > "$LOG"
          {
            sudo apt-get update
            sudo DEBIAN_FRONTEND=noninteractive apt-get install -y \
              git curl wget make gcc g++ bison flex bc cpio \
              python3 python-is-python3 gawk zip unzip rsync patch perl \
              libssl-dev libelf-dev device-tree-compiler file
            echo
            echo "gcc:"
            gcc --version | sed -n '1p'
            echo "make:"
            make --version | sed -n '1p'
            echo "python:"
            python3 --version
          } > "$LOG" 2>&1
          STATUS=$?
          tail -100 "$LOG" || true
          exit "$STATUS"

      - name: 04 - Download Android Clang r547379
        shell: bash
        run: |
          set +e
          LOG="$DIAG_DIR/04-download-clang.log"
          : > "$LOG"
          {
            rm -rf clang-prebuilt toolchain
            git clone --depth=1 --filter=blob:none --sparse \
              --branch android16-qpr2-release \
              https://android.googlesource.com/platform/prebuilts/clang/host/linux-x86.git \
              clang-prebuilt
            cd clang-prebuilt
            git sparse-checkout set clang-r547379
            cd ..
            test -x "$CLANG_DIR/bin/clang"
            test -x "$CLANG_DIR/bin/ld.lld"
            ln -s "$CLANG_DIR" toolchain
            "$CLANG_DIR/bin/clang" --version
            "$CLANG_DIR/bin/ld.lld" --version
          } > "$LOG" 2>&1
          STATUS=$?
          tail -100 "$LOG" || true
          exit "$STATUS"

      - name: 05 - Download AArch64 GCC 4.9
        shell: bash
        run: |
          set +e
          LOG="$DIAG_DIR/05-download-gcc64.log"
          : > "$LOG"
          {
            rm -rf gcc64
            git clone --depth=1 \
              https://github.com/LineageOS/android_prebuilts_gcc_linux-x86_aarch64_aarch64-linux-android-4.9.git \
              gcc64
            test -x gcc64/bin/aarch64-linux-android-gcc
            gcc64/bin/aarch64-linux-android-gcc --version
          } > "$LOG" 2>&1
          STATUS=$?
          tail -80 "$LOG" || true
          exit "$STATUS"

      - name: 06 - Download ARM32 GCC 4.9
        shell: bash
        run: |
          set +e
          LOG="$DIAG_DIR/06-download-gcc32.log"
          : > "$LOG"
          {
            rm -rf gcc32
            git clone --depth=1 \
              https://github.com/LineageOS/android_prebuilts_gcc_linux-x86_arm_arm-linux-androideabi-4.9.git \
              gcc32
            test -x gcc32/bin/arm-linux-androideabi-gcc
            gcc32/bin/arm-linux-androideabi-gcc --version
          } > "$LOG" 2>&1
          STATUS=$?
          tail -80 "$LOG" || true
          exit "$STATUS"

      - name: 07 - Prepare Toolchain Environment
        shell: bash
        run: |
          set +e
          LOG="$DIAG_DIR/07-toolchain-check.log"
          : > "$LOG"
          {
            echo "=================================="
            echo "TOOLCHAIN ENVIRONMENT"
            echo "=================================="

            # IMPORTANT: GITHUB_PATH affects later steps, not the current step.
            # Export PATH here as well so ld.lld is available immediately.
            export PATH="$CLANG_DIR/bin:$GCC64_DIR/bin:$GCC32_DIR/bin:$PATH"

            echo "$CLANG_DIR/bin" >> "$GITHUB_PATH"
            echo "$GCC64_DIR/bin" >> "$GITHUB_PATH"
            echo "$GCC32_DIR/bin" >> "$GITHUB_PATH"

            echo "CROSS_COMPILE=$GCC64_DIR/bin/aarch64-linux-android-" >> "$GITHUB_ENV"
            echo "CROSS_COMPILE_ARM32=$GCC32_DIR/bin/arm-linux-androideabi-" >> "$GITHUB_ENV"

            echo
            echo "PATH:"
            echo "$PATH"

            echo
            echo "CLANG:"
            command -v clang
            clang --version | sed -n '1,2p'

            echo
            echo "LLD:"
            command -v ld.lld
            ld.lld --version | sed -n '1,2p'

            echo
            echo "AARCH64 GCC:"
            command -v aarch64-linux-android-gcc
            aarch64-linux-android-gcc --version | sed -n '1p'

            echo
            echo "ARM32 GCC:"
            command -v arm-linux-androideabi-gcc
            arm-linux-androideabi-gcc --version | sed -n '1p'

            echo
            echo "Exact tool files:"
            ls -lh "$CLANG_DIR/bin/clang" "$CLANG_DIR/bin/ld.lld"

            echo
            echo "[PASS] Toolchain Ready"
          } > "$LOG" 2>&1
          STATUS=$?
          cat "$LOG"
          exit "$STATUS"

      # ONLY FEATURE 1: official KernelSU for this non-GKI 4.19 kernel.
      - name: 08 - Apply KernelSU v0.9.5
        shell: bash
        run: |
          set +e
          LOG="$DIAG_DIR/08-kernelsu.log"
          : > "$LOG"
          {
            curl -LSs \
              "https://raw.githubusercontent.com/tiann/KernelSU/main/kernel/setup.sh" \
              | bash -s "$KSU_VERSION"
            test -d KernelSU
            test -f KernelSU/kernel/Kconfig
            echo "KernelSU version requested: $KSU_VERSION"
            git -C KernelSU describe --tags --always 2>/dev/null || true
          } > "$LOG" 2>&1
          STATUS=$?
          tail -120 "$LOG" || true
          exit "$STATUS"

      # ONLY FEATURE 2: official SUSFS kernel-4.19 release.
      # This explicitly downloads the SUSFS files that were missing in the
      # previous run instead of expecting include/linux/susfs_def.h to exist.
      - name: 09 - Download and Apply SUSFS 1.4.2 for Kernel 4.19
        shell: bash
        run: |
          set +e
          LOG="$DIAG_DIR/09-susfs.log"
          : > "$LOG"
          {
            rm -rf susfs4ksu
            git clone --depth=1 --branch "$SUSFS_VERSION" \
              https://gitlab.com/simonpunk/susfs4ksu.git susfs4ksu

            test -f "susfs4ksu/kernel_patches/KernelSU/10_enable_susfs_for_ksu.patch"
            test -f "susfs4ksu/kernel_patches/KernelSU/kernel/sucompat.h"
            test -f "susfs4ksu/kernel_patches/50_add_susfs_in_kernel-4.19.patch"
            test -f "susfs4ksu/kernel_patches/fs/susfs.c"
            test -f "susfs4ksu/kernel_patches/include/linux/susfs.h"
            test -f "susfs4ksu/kernel_patches/fs/sus_su.c"
            test -f "susfs4ksu/kernel_patches/include/linux/sus_su.h"

            cp -f susfs4ksu/kernel_patches/KernelSU/10_enable_susfs_for_ksu.patch KernelSU/
            cp -f susfs4ksu/kernel_patches/KernelSU/kernel/sucompat.h KernelSU/kernel/
            cp -f susfs4ksu/kernel_patches/50_add_susfs_in_kernel-4.19.patch ./50_add_susfs_in_kernel.patch
            cp -f susfs4ksu/kernel_patches/fs/susfs.c fs/
            cp -f susfs4ksu/kernel_patches/include/linux/susfs.h include/linux/
            cp -f susfs4ksu/kernel_patches/fs/sus_su.c fs/
            cp -f susfs4ksu/kernel_patches/include/linux/sus_su.h include/linux/

            cd KernelSU
            patch -p1 --forward < 10_enable_susfs_for_ksu.patch
            cd ..

            patch -p1 --forward < 50_add_susfs_in_kernel.patch

            echo
            echo "========== SUSFS FILES INSTALLED =========="
            ls -lh fs/susfs.c fs/sus_su.c include/linux/susfs.h include/linux/sus_su.h
            echo
            echo "========== SUSFS PATCH STATUS =========="
            git status --short
            echo
            echo "========== SUSFS VERSION =========="
            grep -Rni "SUSFS_VERSION" include/linux KernelSU 2>/dev/null | head -20 || true
          } > "$LOG" 2>&1
          STATUS=$?
          tail -200 "$LOG" || true
          exit "$STATUS"

      - name: 10 - Configure Stock Defconfig + KernelSU + SUSFS
        shell: bash
        run: |
          set +e
          LOG="$DIAG_DIR/10-defconfig-and-susfs-config.log"
          : > "$LOG"
          {
            export PATH="$CLANG_DIR/bin:$GCC64_DIR/bin:$GCC32_DIR/bin:$PATH"
            export CROSS_COMPILE="$GCC64_DIR/bin/aarch64-linux-android-"
            export CROSS_COMPILE_ARM32="$GCC32_DIR/bin/arm-linux-androideabi-"

            DEFCONFIG="${{ github.event.inputs.defconfig }}"
            test -f "arch/arm64/configs/${DEFCONFIG}"

            make ARCH=arm64 LLVM=1 LLVM_IAS=1 \
              CROSS_COMPILE="$CROSS_COMPILE" \
              CROSS_COMPILE_ARM32="$CROSS_COMPILE_ARM32" \
              "$DEFCONFIG"

            test -s .config

            # SUSFS documentation explicitly recommends disabling SUS_SU when
            # the kernel uses non-kprobe hooks. We do not enable that optional
            # SUSFS root-shell feature; SUSFS itself remains enabled.
            if [ -x scripts/config ]; then
              scripts/config --enable CONFIG_KSU
              scripts/config --enable CONFIG_KSU_SUSFS
              scripts/config --disable CONFIG_KSU_SUSFS_SUS_SU 2>/dev/null || true
            else
              sed -i 's/^# CONFIG_KSU is not set$/CONFIG_KSU=y/' .config || true
              sed -i 's/^# CONFIG_KSU_SUSFS is not set$/CONFIG_KSU_SUSFS=y/' .config || true
              sed -i 's/^CONFIG_KSU_SUSFS_SUS_SU=y$/# CONFIG_KSU_SUSFS_SUS_SU is not set/' .config || true
            fi

            make ARCH=arm64 LLVM=1 LLVM_IAS=1 \
              CROSS_COMPILE="$CROSS_COMPILE" \
              CROSS_COMPILE_ARM32="$CROSS_COMPILE_ARM32" \
              olddefconfig

            echo
            echo "========== REQUIRED CONFIG =========="
            grep -E '^CONFIG_(KSU|KSU_SUSFS|KPROBES|KPROBE_EVENTS|SECURITY|SECURITY_SELINUX)=' .config || true

            grep -q '^CONFIG_KSU=y$' .config
            grep -q '^CONFIG_KSU_SUSFS=y$' .config

            echo
            echo "[PASS] Stock defconfig + KernelSU + SUSFS configuration verified."
          } > "$LOG" 2>&1
          STATUS=$?
          tail -160 "$LOG" || true
          exit "$STATUS"

      - name: 11 - SUSFS Integration Verification
        shell: bash
        run: |
          set +e
          LOG="$DIAG_DIR/11-susfs-verification.log"
          : > "$LOG"
          {
            echo "========== KSU/SUSFS SOURCE CHECK =========="
            test -d KernelSU
            test -f KernelSU/kernel/Kconfig
            test -f fs/susfs.c
            test -f fs/sus_su.c
            test -f include/linux/susfs.h
            test -f include/linux/sus_su.h
            test -f 50_add_susfs_in_kernel.patch
            echo "KernelSU: present"
            echo "SUSFS kernel files: present"
            echo
            echo "Checking for the previous missing header failure:"
            if [ -f include/linux/susfs_def.h ]; then
              echo "susfs_def.h exists (not required by the 1.4.2 file set)."
            else
              echo "susfs_def.h is not expected from the 1.4.2 kernel-4.19 release."
            fi
            echo
            echo "Checking task_mmu SUSFS references:"
            grep -nE 'susfs|INODE_STATE_SUS' fs/proc/task_mmu.c 2>/dev/null || true
            echo
            echo "Git diff summary (feature integration only):"
            git diff --stat || true
          } > "$LOG" 2>&1
          STATUS=$?
          tail -160 "$LOG" || true
          exit "$STATUS"

      # The build is intentionally isolated and fully logged.
      - name: 12 - Build Kernel (Full Log to Artifact)
        shell: bash
        run: |
          set +e
          FULL_LOG="$GITHUB_WORKSPACE/kernel-build-full.log"
          ERROR_LOG="$DIAG_DIR/12-build-errors.log"
          TAIL_LOG="$DIAG_DIR/12-build-tail.log"
          SUMMARY_LOG="$DIAG_DIR/12-build-summary.log"

          : > "$FULL_LOG"
          : > "$ERROR_LOG"
          : > "$TAIL_LOG"
          : > "$SUMMARY_LOG"

          export PATH="$CLANG_DIR/bin:$GCC64_DIR/bin:$GCC32_DIR/bin:$PATH"
          export CROSS_COMPILE="$GCC64_DIR/bin/aarch64-linux-android-"
          export CROSS_COMPILE_ARM32="$GCC32_DIR/bin/arm-linux-androideabi-"

          echo "=========================================="
          echo "STARTING STOCK + KERNELSU + SUSFS BUILD"
          echo "Full V=1 stdout/stderr -> kernel-build-full.log"
          echo "=========================================="
          echo "clang=$(command -v clang)"
          echo "ld.lld=$(command -v ld.lld)"
          clang --version | sed -n '1,2p'
          ld.lld --version | sed -n '1,2p'

          START=$(date +%s)

          make \
            ARCH=arm64 \
            LLVM=1 \
            LLVM_IAS=1 \
            CROSS_COMPILE="$CROSS_COMPILE" \
            CROSS_COMPILE_ARM32="$CROSS_COMPILE_ARM32" \
            -j"$(nproc)" \
            V=1 \
            WERROR=0 \
            >"$FULL_LOG" 2>&1

          BUILD_STATUS=$?
          END=$(date +%s)
          ELAPSED=$((END-START))

          {
            echo "BUILD_STATUS=$BUILD_STATUS"
            echo "BUILD_TIME_SECONDS=$ELAPSED"
            echo "BUILD_TIME_HUMAN=$((ELAPSED/60))m $((ELAPSED%60))s"
            echo "BUILD_LOG_BYTES=$(wc -c < "$FULL_LOG")"
            echo "BUILD_LOG_LINES=$(wc -l < "$FULL_LOG")"
          } | tee "$SUMMARY_LOG"

          grep -nEi \
            'fatal error:|error:|undefined reference|undeclared|implicit declaration|invalid|unknown argument|unknown warning|No such file|No rule to make target|recipe for target|Error [0-9]+|make\[[0-9]+\]: \*\*\*' \
            "$FULL_LOG" | tail -250 > "$ERROR_LOG" || true

          tail -300 "$FULL_LOG" > "$TAIL_LOG" || true

          echo
          echo "===== COMPACT BUILD ERROR SUMMARY ====="
          if [ -s "$ERROR_LOG" ]; then
            tail -30 "$ERROR_LOG" || true
          else
            echo "No compiler-error pattern found."
          fi

          echo
          echo "===== LAST 40 BUILD LINES ====="
          tail -40 "$FULL_LOG" || true

          if [ "$BUILD_STATUS" -eq 0 ]; then
            test -s arch/arm64/boot/Image
            echo "KERNEL BUILD SUCCESSFUL"
          else
            echo "KERNEL BUILD FAILED - FULL LOG SAVED AS ARTIFACT"
          fi

          exit "$BUILD_STATUS"

      - name: 13 - Final Configuration and Artifact Check
        if: always()
        shell: bash
        run: |
          set +e
          LOG="$DIAG_DIR/13-final-check.log"
          : > "$LOG"
          {
            echo "========== FINAL CHECK =========="
            date -u
            echo
            echo "Kernel version:"
            grep -E '^(VERSION|PATCHLEVEL|SUBLEVEL|EXTRAVERSION)[[:space:]]*=' Makefile || true
            echo
            echo "Required config:"
            grep -E '^CONFIG_(KSU|KSU_SUSFS|KPROBES|KPROBE_EVENTS|SECURITY|SECURITY_SELINUX)=' .config 2>/dev/null || true
            echo
            echo "Kernel image:"
            ls -lh arch/arm64/boot/Image 2>&1 || true
            echo
            echo "Build errors (last 40):"
            tail -40 "$DIAG_DIR/12-build-errors.log" 2>/dev/null || true
            echo
            echo "Build tail (last 40):"
            tail -40 "$DIAG_DIR/12-build-tail.log" 2>/dev/null || true
          } > "$LOG" 2>&1
          cat "$LOG"
          exit 0

      - name: 14 - Stage Complete Diagnostics
        if: always()
        shell: bash
        run: |
          set +e
          mkdir -p diagnostic-artifact
          cp -f kernel-build-full.log diagnostic-artifact/12-build-full.log 2>/dev/null || true
          cp -f .config diagnostic-artifact/final.config 2>/dev/null || true
          cp -f 50_add_susfs_in_kernel.patch diagnostic-artifact/50_add_susfs_in_kernel.patch 2>/dev/null || true
          cp -rf "$DIAG_DIR" diagnostic-artifact/ci-logs 2>/dev/null || true
          {
            echo "GitHub Run: $GITHUB_RUN_ID"
            echo "Commit: $GITHUB_SHA"
            echo "Date: $(date -u)"
            echo "KernelSU: $KSU_VERSION"
            echo "SUSFS: $SUSFS_VERSION"
            echo
            find diagnostic-artifact -type f -printf '%p %s bytes\n' | sort
          } > diagnostic-artifact/MANIFEST.txt
          exit 0

      - name: 15 - Upload Complete Diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: LG-Velvet-Stock-KSU-SUSFS-Diagnostics
          path: diagnostic-artifact
          if-no-files-found: error
          retention-days: 14
          compression-level: 6

      - name: 16 - Upload Kernel Image
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: LG-Velvet-Stock-KSU-SUSFS-Image
          path: arch/arm64/boot/Image
          if-no-files-found: error
          retention-days: 14
